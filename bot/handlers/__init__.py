"""
The handlers package for the Telegram GPT bot.

- Aggregates all command and conversation handlers (e.g., /start, /help, /quiz, /talk, /paraphrase).
- Each handler module defines logic for a specific bot command or feature.
- Used by the bot’s main entry point to register commands and organize bot functionality.

Пакет handlers для Telegram-бота GPT.
- Содержит обработчики всех команд и сценариев общения с пользователем (/start, /help, /quiz, /talk, /paraphrase).
- Каждый модуль реализует отдельную логику команды.
- Используется для организации команд и регистрации обработчиков в основном файле.
"""